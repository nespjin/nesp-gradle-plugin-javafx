/*
 * Copyright (C) 2021 The NESP Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package com.nesp.gradle.plugin.javafx.fxml;

/**
 * Team: NESP Technology
 * Author: <a href="mailto:1756404649@qq.com">JinZhaolu Email:1756404649@qq.com</a>
 * Time: Created 2021/8/4 23:44
 * Project: nesp-gradle-plugin-javafx
 * Description:
 * <p>
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 *
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 * The modifier {@code public}           PUBLIC,
 * The modifier {@code protected}  The modifier {@code private}  The modifier {@code abstract}
 * The modifier {@code default}
 * @since 1.8
 * The modifier {@code static}
 * The modifier {@code sealed}
 * @since 17
 * <p>
 * The modifier {@code non-sealed}
 * @since 17
 * The modifier {@code final}  The modifier {@code transient}  The modifier {@code volatile}  The modifier {@code synchronized}  The modifier {@code native}
 */


/** The modifier {@code public}           PUBLIC,
 *//** The modifier {@code protected} *//*       PROTECTED,
 *//** The modifier {@code private} *//*         PRIVATE,
 *//** The modifier {@code abstract} *//*        ABSTRACT,
 *//**
 * The modifier {@code default}
 * @since 1.8
 *//*
        DEFAULT,
        *//** The modifier {@code static} *//*          STATIC,

 *//**
 * The modifier {@code sealed}
 * @since 17
 *//*
        SEALED,

        *//**
 * The modifier {@code non-sealed}
 * @since 17
 *//*
        NON_SEALED {
public String toString() {
        return "non-sealed";
        }
        },
        *//** The modifier {@code final} *//*           FINAL,
 *//** The modifier {@code transient} *//*       TRANSIENT,
 *//** The modifier {@code volatile} *//*        VOLATILE,
 *//** The modifier {@code synchronized} *//*    SYNCHRONIZED,
 *//** The modifier {@code native} *//*          NATIVE,
 */

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

/** The modifier {@code strictfp} *//*        STRICTFP;*/
public enum Modifier {
    PUBLIC("public"), /* 0000 0000 0000 0001 */
    PROTECTED("protect"), /* 0000 0000 0000 0010 */
    PRIVATE("private"), /* 0000 0000 0000 0100 */
    ABSTRACT("abstract"), /* 0000 0000 0000 1000 */
    DEFAULT("default"), /* 0000 0000 0001 0000 */
    STATIC("static"), /* 0000 0000 0010 0000 */
    SEALED("sealed"), /* 0000 0000 0100 0000 */
    NON_SEALED("non_sealed"), /* 0000 0000 1000 0000 */
    FINAL("final"), /* 0000 0001 0000 0000 */
    TRANSIENT("transient"), /* 0000 0010 0000 0000 */
    VOLATILE("volatile"), /* 0000 0100 0000 0000 */
    SYNCHRONIZED("synchronized"), /* 0000 1000 0000 0000 */
    NATIVE("native"), /* 0001 0000 0000 0000 */
    STRICTFP("strictfp"), /* 0010 0000 0000 0000 */
    PACKAGE(""), /* 0100 0000 0000 0000 */;

    private int value;
    private String name;

    Modifier(String name) {
        this.value = 1 << (ordinal() + 1);
        this.name = name;
    }

    public String getName() {
        return name;
    }

    public Modifier setName(final String name) {
        this.name = name;
        return this;
    }

    public int getValue() {
        return value;
    }

    public static List<Integer> toReflectModifiers(List<Modifier> modifiers) {
        if (modifiers == null || modifiers.isEmpty()) return Collections.emptyList();
        final List<Integer> reflectModifiers = new ArrayList<>();
        for (final Modifier modifier : modifiers) {
            reflectModifiers.add(modifier.toReflectModifier());
        }
        return reflectModifiers;
    }

    public int toReflectModifier() {
        switch (this) {
            case PUBLIC -> {
                return java.lang.reflect.Modifier.PUBLIC;
            }
            case PROTECTED -> {
                return java.lang.reflect.Modifier.PROTECTED;
            }
            case PRIVATE -> {
                return java.lang.reflect.Modifier.PRIVATE;
            }
            case ABSTRACT -> {
                return java.lang.reflect.Modifier.ABSTRACT;
            }
//            case DEFAULT -> {
//                return java.lang.reflect.Modifier.DEFAULT;
//            }
            case STATIC -> {
                return java.lang.reflect.Modifier.STATIC;
            }
//            case SEALED -> {
//                return java.lang.reflect.Modifier.SEALED;
//            }
//            case NON_SEALED -> {
//                return java.lang.reflect.Modifier.NON_SEALED;
//            }
            case FINAL -> {
                return java.lang.reflect.Modifier.FINAL;
            }
            case TRANSIENT -> {
                return java.lang.reflect.Modifier.TRANSIENT;
            }
            case VOLATILE -> {
                return java.lang.reflect.Modifier.VOLATILE;
            }
            case SYNCHRONIZED -> {
                return java.lang.reflect.Modifier.SYNCHRONIZED;
            }
            case NATIVE -> {
                return java.lang.reflect.Modifier.NATIVE;
            }
//            case STRICTFP -> {
//                return java.lang.reflect.Modifier.STRICTFP;
//            }
//            case PACKAGE -> {
//                return java.lang.reflect.Modifier.PACKAGE;
//            }
        }
        throw new IllegalArgumentException("Unsupported " + this);
    }

    public static List<javax.lang.model.element.Modifier> toVmModifiers(List<Modifier> modifiers) {
        if (modifiers == null || modifiers.isEmpty()) return Collections.emptyList();
        final List<javax.lang.model.element.Modifier> vmModifiers = new ArrayList<>();
        for (final Modifier modifier : modifiers) {
            vmModifiers.add(modifier.toVmModifier());
        }
        return vmModifiers;
    }

    public javax.lang.model.element.Modifier toVmModifier() {
        switch (this) {
            case PUBLIC -> {
                return javax.lang.model.element.Modifier.PUBLIC;
            }
            case PROTECTED -> {
                return javax.lang.model.element.Modifier.PROTECTED;
            }
            case PRIVATE -> {
                return javax.lang.model.element.Modifier.PRIVATE;
            }
            case ABSTRACT -> {
                return javax.lang.model.element.Modifier.ABSTRACT;
            }
//            case DEFAULT -> {
//                return javax.lang.model.element.Modifier.DEFAULT;
//            }
            case STATIC -> {
                return javax.lang.model.element.Modifier.STATIC;
            }
//            case SEALED -> {
//                return javax.lang.model.element.Modifier.SEALED;
//            }
//            case NON_SEALED -> {
//                return javax.lang.model.element.Modifier.NON_SEALED;
//            }
            case FINAL -> {
                return javax.lang.model.element.Modifier.FINAL;
            }
            case TRANSIENT -> {
                return javax.lang.model.element.Modifier.TRANSIENT;
            }
            case VOLATILE -> {
                return javax.lang.model.element.Modifier.VOLATILE;
            }
            case SYNCHRONIZED -> {
                return javax.lang.model.element.Modifier.SYNCHRONIZED;
            }
            case NATIVE -> {
                return javax.lang.model.element.Modifier.NATIVE;
            }
//            case STRICTFP -> {
//                return javax.lang.model.element.Modifier.STRICTFP;
//            }
//            case PACKAGE -> {
//                return javax.lang.model.element.Modifier.PACKAGE;
//            }
        }
        throw new IllegalArgumentException("Unsupported " + this);
    }

    public static List<Modifier> fromReflectModifiers(int mod) {
        final List<Modifier> modifiers = new ArrayList<>();
        if (java.lang.reflect.Modifier.isPublic(mod)) {
            modifiers.add(PUBLIC);
        } else if (java.lang.reflect.Modifier.isProtected(mod)) {
            modifiers.add(PROTECTED);
        } else if (java.lang.reflect.Modifier.isPrivate(mod)) {
            modifiers.add(PRIVATE);
        } else if (java.lang.reflect.Modifier.isAbstract(mod)) {
            modifiers.add(ABSTRACT);
        }/* else if (java.lang.reflect.Modifier.isDefault(mod)) {
            modifiers.add(DEFAULT);
        }*/ else if (java.lang.reflect.Modifier.isStatic(mod)) {
            modifiers.add(STATIC);
        }/* else if (java.lang.reflect.Modifier.isSealed(mod)) {
            modifiers.add(SEALED);
        } else if (java.lang.reflect.Modifier.isNonSealed(mod)) {
            modifiers.add(NON_SEALED);
        }*/ else if (java.lang.reflect.Modifier.isFinal(mod)) {
            modifiers.add(FINAL);
        } else if (java.lang.reflect.Modifier.isTransient(mod)) {
            modifiers.add(TRANSIENT);
        } else if (java.lang.reflect.Modifier.isVolatile(mod)) {
            modifiers.add(VOLATILE);
        } else if (java.lang.reflect.Modifier.isSynchronized(mod)) {
            modifiers.add(SYNCHRONIZED);
        } else if (java.lang.reflect.Modifier.isNative(mod)) {
            modifiers.add(NATIVE);
        }/* else if (java.lang.reflect.Modifier.isStrictfp(mod)) {
            modifiers.add(STRICTFP);
        } else if (java.lang.reflect.Modifier.isPackage(mod)) {
            modifiers.add(PACKAGE);
        }*/
        return modifiers;
    }

    public static boolean isPublic(int mod) {
        return (mod & PUBLIC.getValue()) != 0;
    }

    public boolean isProtect(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isPrivate(int mod) {
        return (mod & PRIVATE.getValue()) != 0;
    }

    public boolean isAbstract(int mod) {
        return (mod & ABSTRACT.getValue()) != 0;
    }

    public boolean isDefault(int mod) {
        return (mod & DEFAULT.getValue()) != 0;
    }

    public boolean isStatic(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isSealed(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isNon_sealed(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isFinal(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isTransient(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isVolatile(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isSynchronized(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isNative(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }

    public boolean isStrictfp(int mod) {
        return (mod & PROTECTED.getValue()) != 0;
    }
}
